import { flexCenter } from "@styled/index.styled"
import styled from "styled-components"

export const AccountST = styled.div`
    ${flexCenter}
    width: 100%;
    height: fit-content;
    font-size: 1.8rem;
    font-family: 500;
`
